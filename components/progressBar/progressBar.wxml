<!--components/progressBar/progressBar.wxml-->
<view class="progress-container">
  <view class="progress-wrapper">
    <!-- 主进度条 -->
    <view class="progress-bar {{isComplete ? 'progress-complete' : ''}}" style="height: {{barHeight}}rpx; background-color: {{backgroundColor}}; border-radius: {{borderRadius}}rpx;">
      <!-- 阶段节点标记 -->
      <block wx:if="{{useGradient}}">
        <view class="stage-node node-25" data-stage="1"><view class="node-dot"></view></view>
        <view class="stage-node node-50" data-stage="2"><view class="node-dot"></view></view>
        <view class="stage-node node-75" data-stage="3"><view class="node-dot"></view></view>
      </block>
      
      <!-- 渐变填充 -->
      <view class="progress-fill" 
            style="width: {{percentage}}%; height: {{barHeight}}rpx; border-radius: {{borderRadius}}rpx; background: {{stageColors[gradientStage]}};">
      </view>
      
      <!-- 终点区域 -->
      <view class="goal-area {{isComplete ? 'goal-complete' : ''}}" wx:if="{{!rewardImage}}">
        <view class="goal-icon">🎁</view>
      </view>
      
      <!-- 使用自定义奖品图片 -->
      <view class="goal-area-image {{isComplete ? 'goal-complete' : ''}}" wx:else>
        <image class="reward-image" src="{{rewardImage}}" mode="aspectFill"></image>
      </view>
      
      <!-- 庆祝礼花效果 -->
      <view class="celebration-effects {{isComplete && showFireworks ? 'show-fireworks' : ''}}">
        <view class="firework firework-1"></view>
        <view class="firework firework-2"></view>
        <view class="firework firework-3"></view>
        <view class="firework firework-4"></view>
        <view class="firework firework-5"></view>
        <view class="confetti-container">
          <view class="confetti confetti-1"></view>
          <view class="confetti confetti-2"></view>
          <view class="confetti confetti-3"></view>
          <view class="confetti confetti-4"></view>
          <view class="confetti confetti-5"></view>
          <view class="confetti confetti-6"></view>
          <view class="confetti confetti-7"></view>
          <view class="confetti confetti-8"></view>
        </view>
      </view>
      
      <!-- 小黄鸡角色 -->
      <view wx:if="{{showChick}}" 
            class="chick-character {{chickState}} {{chickAnimation ? chickAnimation : ''}}" 
            style="left: {{percentage}}%;" 
            bindtap="onTapChick">
        
        <!-- 小鸡角色 -->
        <view class="chick-wrapper">
          <!-- 基础小鸡 -->
          <text class="chick-emoji">🐥</text>
        </view>
        
        <!-- 鼓励语气泡 -->
        <view class="encouragement-bubble {{chickSpeaking ? 'show' : ''}} {{nodeReached ? 'highlight' : ''}}">
          <text>{{currentEncouragement}}</text>
        </view>
      </view>
    </view>
  </view>
</view> 