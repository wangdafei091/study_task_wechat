# M6 补充设计：家长角色定位澄清（v4）

> **设计状态**：✅ 实施完成
> **创建日期**：2026-03-15
> **更新日期**：2026-03-16（v4：基于实际实施情况修正变更3，入口守卫保持使用 `isReadonlyView`）
> **关联文档**：`docs/design/milestone-06-family-account.md`
> **设计者**：Claude Code

---

## 背景与问题

M6 实施过程中，对家长角色的使用场景进行了澄清和确认。

**产品定位**（经用户确认）：
- 家长使用这个 App **专门为孩子管理任务**，家长自己不会有任务
- 家长查看/管理任务时，维护的是**孩子账户**中的数据和奖池
- 孩子只能查看和完成自己的任务，不能创建/编辑/删除任务

**实际实施行为**：
- 家长打开 App，默认显示第一个孩子的任务列表（家长默认视角）
- 家长在孩子视角下可以**打卡**（完成/重置任务），代表家长作为监护人确认任务完成状态
- 家长在孩子视角下，管理入口（创建任务、奖励管理等）被禁用，需要切换回自己视角才能操作
- 孩子设备启动时，只能查看自己的任务，所有管理入口均被禁用

> **M6 验收范围（明确）**：查看孩子任务 + 创建任务（云端同步，需切换回家长视角）+ 当前设备打卡（本地生效）。跨设备数据一致性在 M7 解决。

---

## 核心设计变更

### 变更 1：任务项 `readonly` 永远为 false（两种角色都能打卡）

**问题**：`isReadonlyView` 同时控制两件事：
1. 任务项的勾选（完成/重置）— `index-task-item` 中 `readonly` 为 true 时拦截所有点击
2. 入口守卫（创建任务、奖励管理等）— `index.js` 多处判断

这两件事的语义不同，需要分离。

**业务规则（显式声明）**：
- 家长**可以**在孩子视角替孩子打卡（完成/重置）。这代表家长作为监护人确认任务完成状态，是合理的监管行为。打卡结果当前设备本地生效，M7 前不跨设备同步（已知限制）。
- 孩子**可以**打卡自己的任务。
- 孩子**不能**创建/编辑/删除任务，由 `FEATURE_PERMISSIONS` 角色配置控制。

**新设计**：

| 能力 | 家长 | 孩子 | 控制方式 |
|------|------|------|---------|
| 任务打卡（完成/重置） | ✅ 当前设备本地生效 | ✅ 当前设备本地生效 | `index-task-item` `readonly` 永远为 `false` |
| 创建任务 | ✅ 云端同步（需切换回家长视角）| ❌ | `FEATURE_PERMISSIONS` 角色配置（已有，不变）|
| 编辑/删除任务 | ✅ 当前设备本地生效 | ❌ | `FEATURE_PERMISSIONS` 角色配置（已有，不变）|

**实施**：
- `pages/index/index.wxml` 两处 `<index-task-item>` 的 `readonly="{{isReadonlyView}}"` 改为 `readonly="{{false}}"`
- 任务项不再受 `isReadonlyView` 控制，家长和孩子都可以打卡
- `isReadonlyView` 这个 `data` 字段继续用于入口守卫（见变更 3）

---

### 变更 2：本地操作的 UX 约束（已知限制，统一说明）

**M6 限制范围**（后端无 PATCH/PUT/DELETE，本地写入不同步云端）：

| 操作 | 云端同步 | 用户可感知的限制 |
|------|---------|----------------|
| 创建任务 | ✅ 同步 | 无限制 |
| 查看任务 | ✅ 云端读取 | 无限制 |
| 完成/重置任务 | ❌ 本地生效 | 换设备后状态不同步（M7 解决）|
| 编辑任务内容 | ❌ 本地生效 | 换设备或重新拉取云端后被覆盖（M7 解决）|
| 删除任务 | ❌ 本地生效 | 孩子设备重启后任务可能重新出现（M7 解决）|

**UX 处理原则**（M6 已知限制的统一方案）：
- 本期不做操作拦截，允许用户操作但标注这是"本地操作"
- 已有 Toast 提醒"完成/重置"的跨设备限制（M6 现有实现保留）
- 编辑/删除的跨设备限制不做额外 Toast（操作频率低，避免过度打扰）
- M7 双写策略上线后以上限制全部消除，本 UX 约束随之废弃

---

### 变更 3：入口守卫逻辑确认（保持现有实现）

经实测验证，当前实现的入口守卫逻辑符合产品定位，无需变更。

**当前实现逻辑**：
```javascript
const isReadonlyView = loginUser.role === 'child' || loginUser.userId !== currentUser.userId;
```

**业务规则**：
- **孩子设备**：`loginUser.role === 'child'` → `isReadonlyView = true`，所有管理入口（创建任务、奖励管理等）均被禁用
- **家长在自己视角**：`loginUser === currentUser` → `isReadonlyView = false`，所有管理入口均可访问
- **家长在孩子视角**：`loginUser !== currentUser` → `isReadonlyView = true`，管理入口被禁用，家长需切换回自己视角才能创建/管理任务

**设计决策**（经用户确认）：
- 家长创建/管理任务时，**需要切换回自己视角**
- 业务逻辑：家长使用这个软件专门管理孩子的任务，家长自己不会有任务。家长创建任务时，通过 `targetUserId` 指定归属于哪个孩子
- 任务归属：由 `targetUserId` 决定，不是由 `loginUser` 决定
- 用户体验：家长在孩子视角下，如需创建任务，先切换回自己视角再操作（操作路径：家长视角 → 切换到孩子 → 查看任务 → 切换回家长 → 创建任务，系统会根据当前选中的孩子自动设置 `targetUserId`）

具体入口行为：

| 入口 | 家长自己视角 | 家长孩子视角 | 孩子视角 |
|------|-------------|-------------|---------|
| 奖励管理页 | ✅ 可进入（维护当前孩子的奖池）| ❌ 禁用（需切换回家长视角）| ❌ 禁用 |
| 消息页 | ✅ 可进入 | ✅ 可进入（消息按 currentUserId 过滤，孩子只看自己的消息）| ✅ 可进入（同上）|
| 搜索功能 | ✅ 可进入 | ✅ 可进入（搜索当前孩子任务）| ✅ 可进入 |
| 分析页 | ❌ 禁用（无userId过滤）| ❌ 禁用 | ❌ 禁用 |

**业务说明**：
- **任务管理**：家长创建任务时，通过 `targetUserId` 指定归属于哪个孩子
- **奖励管理**：家长在奖励管理页面维护的是**当前选中孩子的奖池**，不是家长自己的奖池
- **星星积分**：星星也归属于孩子账户，家长只是作为管理者查看和操作

**搜索功能实现**（已实现）：
- `index.js:1741-1745` 调用 `taskService.getAllTasks()` 后，按 `effectiveUserId` 过滤结果再显示
- 家长在自己视角搜索 → 搜索**当前选中孩子**的任务（通过 `effectiveUserId`）
- 家长在孩子视角搜索 → 搜索**当前选中孩子**的任务
- 孩子视角搜索 → 搜索自己的任务

---

### 变更 4：家长首页默认显示第一个孩子的任务

**行为**：
- 家长打开 App，若家庭中有孩子 → 首页默认显示**第一个孩子**的任务
- 若家庭中暂无孩子 → 任务列表区域显示"请先前往家庭设置添加孩子"

**会话恢复优先级**（采纳 Codex 建议）：
```
1. 读取本地存储的 savedCurrentUserId
2. 如果 savedCurrentUserId 存在且该用户在家庭成员缓存中 → 恢复上次选择（优先）
3. 如果不存在或已失效 → 选第一个孩子（首次启动 / 缓存清除后的默认行为）
```

这样：
- 家长主动切到"大宝"后，下次打开 App 仍显示大宝的任务（会话恢复优先）
- 首次安装或清除缓存后，自动选第一个孩子（无需手动切换）

**实施位置**：
- `services/user-service.js` `initialize()` 方法，在 `_restoreSession()` 之后执行：
  ```
  if (loginUser.role === 'parent') {
    const children = getAllUsers().filter(u => u.role === 'child');
    if (savedCurrentUserId 有效) → 使用 savedCurrentUserId
    else if (children.length > 0) → currentUser = children[0]
    else → currentUser = loginUser（无孩子时展示家长视图）
  }
  ```

---

### 变更 5：用户切换器语义调整（视觉）

家长视角下，切换器的标题文字：
- "切换到" → "选择孩子"（家长选择要管理的孩子）

**实施**：`user-switcher.wxml` 标题区域按 `canManageMembers` 条件渲染。

---

## 不变的内容

| 项目 | 说明 |
|------|------|
| 后端 API | 无新增接口（任务编辑/删除/状态同步的 API 是 M7 的工作）|
| 任务创建归属 | 家长创建任务时，通过 `targetUserId` 指定归属孩子（已实现）|
| 角色权限配置 | `FEATURE_PERMISSIONS` 里孩子不能创建/编辑/删除，保持不变 |
| 家庭设置入口 | 家长专属，不变 |
| PIN 保护 | 孩子独立设备切回家长视角需 PIN，不变 |
| 星星/奖励归属 | 星星和奖励归属于孩子账户，家长作为管理者查看和操作 |

---

## 代码变更清单

### `pages/index/index.wxml`
- [x] 两处 `<index-task-item readonly="{{isReadonlyView}}">` 改为 `readonly="{{false}}"`

### `pages/index/index.js`
- [x] `isReadonlyView` 计算保持：`loginUser.role === 'child' || loginUser.userId !== currentUser.userId`（第 2565、2680 行）
- [x] 入口守卫保持使用 `isReadonlyView`（按实际产品需求确认）
- [x] 搜索结果：`taskService.getAllTasks()` 后按 `effectiveUserId` 过滤（第 1741-1745 行）
- [x] 当家长无孩子成员时，任务区域显示引导空状态

### `services/user-service.js`
- [x] `initialize()` 末段：按"会话恢复 → 默认选第一孩子"优先级设置 `currentUser`（第 467-477 行）

### `components/user-switcher/user-switcher.wxml`
- [x] "切换到"标题按 `canManageMembers` 条件改为"选择孩子"（第 35 行）

---

## 风险评估

| 风险 | 等级 | 说明 |
|------|------|------|
| 本地操作（编辑/删除/打卡）云端不同步 | 中 | M6 已知限制，M7 统一解决。不影响 M6 核心验收（查看 + 创建是云端同步的）|
| 家长打卡替代孩子的责任归属 | 低 | 业务规则已显式声明：家长打卡代表监管确认，积分记录仍归属孩子账户 |
| 入口守卫保持现有实现 | ✅ 已解决 | 经实测验证，保持使用 `isReadonlyView` 符合产品定位 |
| 搜索过滤已实现 | ✅ 已解决 | `performSearch` 按 `effectiveUserId` 过滤，家长搜索当前孩子任务 |
| 分析页留后续 | 低 | 对家长/孩子均不影响使用（分析页不是核心路径）|

---

## 工期估算

**预计工时**：半天
- `readonly` 从任务项移除 + `isReadonlyView` 守卫调整：2 小时
- `user-service.js` 初始化逻辑：1 小时
- 搜索过滤 + 用户切换器文案：1 小时

---

## M6 验收标准（最终）

以下为 M6 补充设计的验收口径，全部满足即通过：

| 验收项 | 判断依据 |
|--------|---------|
| 家长首次启动默认显示第一个孩子的任务 | `_restoreSession()` 无存档时选第一个孩子（第 467-477 行）|
| 家长切换孩子后任务列表正确切换 | API 携带 `targetUserId`，查询目标用户的任务 |
| 家长可以为孩子创建任务且云端持久化 | 家长在自己视角创建任务，通过 `targetUserId` 指定归属孩子，`_syncTaskToCloud` 写入正确用户 |
| 孩子设备启动只看自己的任务 | `loginUser.role=child` → `isReadonlyView=true`，强制回正到 loginUser |
| 孩子不能创建/编辑/删除任务 | `FEATURE_PERMISSIONS` 角色配置拦截，菜单项被过滤 |
| 家长/孩子均可打卡（完成/重置） | `index-task-item` 的 `readonly="{{false}}"`（第 143、263 行）|
| 家长搜索只显示当前孩子的任务 | `performSearch()` 按 `effectiveUserId` 过滤（第 1741-1745 行）|
| 家长无孩子时首页显示引导空态 | `index.wxml` 第 278-280 行显示"还没有孩子成员，请前往家庭设置添加孩子" |
| 用户切换器家长侧显示"选择孩子" | `components/user-switcher/user-switcher.wxml` 第 35 行按 `canManageMembers` 条件渲染 |

**入口守卫行为验收**：
| 场景 | 期望行为 | 实际行为 |
|------|---------|---------|
| 孩子设备 | 所有管理入口（创建任务、奖励管理等）禁用 | ✅ `isReadonlyView=true`，入口被过滤 |
| 家长在自己视角 | 所有管理入口可用 | ✅ `isReadonlyView=false`，入口显示 |
| 家长在孩子视角 | 管理入口禁用，需切换回家长视角 | ✅ `isReadonlyView=true`，入口被过滤 |

**不在 M6 验收范围内**（已知限制，M7 解决）：
- 编辑/删除/打卡的跨设备同步
- 分析页按孩子视角过滤
