# 项目规则

## 技术栈
- 微信小程序原生框架，DDD架构
- 核心服务：TaskService、StarService、RewardService、MessageService、UserService、ValidationService
- 工具链：logger、batchUtils、EventBus、StorageAdapter、Jest测试

## 开发规范

### 架构原则
- 遵循DDD分层：领域层(models) → 应用层(services) → 基础设施层(repositories/adapters) → 表现层(pages/components)
- 通过ServiceManager访问服务，避免直接操作存储
- 使用EventBus进行跨服务通信
- 批量操作使用batchUtils避免UI阻塞

### 代码风格
- 使用简洁的中文回复
- 仅修复用户提出的问题，做最小最简单的方案
- 在关键操作处增加logger日志记录
- 简化逻辑，去除冗余代码

### UI规范
- 卡片内边距：30rpx，圆角：16rpx
- 按钮高度：90rpx，圆角：8rpx
- 元素间距：12rpx/24rpx
- 颜色：学习任务#4285F4，习惯任务#4CAF50，兴趣任务#FF9800
- 字体：标题32rpx/500-600，正文28rpx/400，辅助24rpx/400

### 日志规范
使用统一格式：`logger.info('componentName', '操作描述', 变量)`
关键点必须记录：任务状态变更、积分计算、重要数据操作、异步操作

### 性能优化
- 大量数据使用batchUtils批量处理
- 合并setData调用，减少渲染次数
- 异步操作使用Promise/async-await
- 使用setTimeout延迟非关键任务

### 测试要求
- 使用Jest进行单元测试和集成测试
- 测试文件按模块组织在test/目录
- 保持高测试覆盖率（目标85%+）

### 文档管理
- 所有文档存放在docs/目录
- 按类型分类：architecture/、development/、api/、user/
- 代码修改时同步更新相关文档