<view class="star-trend-container">
  <!-- 标题区域 - 不需要在这里显示标题，已经有了顶部标题 -->
  <!-- <view class="chart-title">可用星星趋势图 - 看看你的星星余额</view> -->

  <!-- 筛选控件区域 -->
  <view class="filter-container">
    <view class="filter-options">
      <view class="{{currentRange === 7 ? 'filter-option selected' : 'filter-option'}}" bindtap="onSelectRange" data-days="7">最近7天</view>
      <view class="{{currentRange === 30 ? 'filter-option selected' : 'filter-option'}}" bindtap="onSelectRange" data-days="30">最近30天</view>
    </view>
    <view class="date-range">
      <image class="date-icon" src="/assets/icons/calendar.png"></image>
      <text class="date-range-text">{{dateRangeText}}  (虚线为预测趋势)</text>
    </view>
  </view>

  <!-- 图表容器 - 始终存在 -->
  <view class="chart-container">
    <ec-canvas id="starTrendChart" class="trend-canvas" canvas-id="starTrendChart" ec="{{ ec }}" force-use-old-canvas="{{isSimulator}}"></ec-canvas>
    
    <!-- 状态覆盖层 -->
    <view wx:if="{{isLoading}}" class="status-overlay">
      <view class="loading-text">加载中...</view>
    </view>
    <view wx:elif="{{!hasStarRecords}}" class="status-overlay">
      <view class="no-data-text">完成任务获取星星，这里就会显示你的星星余额变化哦！</view>
    </view>
  </view>
</view> 
